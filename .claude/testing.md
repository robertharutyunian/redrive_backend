# Testing

- **Unit tests**: Vitest (not Jest). Config in `vitest.config.ts`.
- **E2E tests**: separate config in `vitest.config.e2e.ts`.
- Run a single test file: `pnpm vitest run src/path/to/spec.ts`
