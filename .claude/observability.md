# Observability

`@nestjs/observe` is wired into `NestFactory.create` via the `instrument` option in `main.ts`. The app key/secret in `app.module.ts` are placeholders and need real env vars before production use.
